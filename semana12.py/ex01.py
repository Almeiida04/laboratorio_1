def numeros_inteiros():
    numeros = []

    for i in range(8):
        num = float(input("Digite um número: "))
        numeros.append(num)

    return numeros

def maior_numeros(numeros):
    for i in range(len(numeros)):
        if len(numeros) > 30:
            len(numeros)
            print(f"Os números maiores que 30 são: {len(numeros)}")

def soma(numeros):
    somar = sum(numeros)
    print(f"A soma dos números é {numeros} é {soma}")

def soma_todos(numeros):
    somar = 0

def main():
    numeros = 